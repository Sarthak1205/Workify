import 'package:flutter/material.dart';

class BuyPreviewPage extends StatelessWidget {
  const BuyPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("buy page"),
        centerTitle: true,
      ),
    );
  }
}
